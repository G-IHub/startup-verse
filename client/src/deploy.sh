#!/usr/bin/env bash
# StartupVerse — deploy the Node/Express API (see /server in this repo).

set -e
echo ""
echo "Run the API from the server package, for example:"
echo "  cd ../../server && npm install && npm run start"
echo ""
echo "Set VITE_API_URL in the client to match your deployed API base URL."
