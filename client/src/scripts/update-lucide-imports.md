# Lucide React Import Update Script

This document lists all files that need lucide-react import updates from `'lucide-react'` to `'lucide-react'`

## Files Already Updated:

- ✅ /App.tsx
- ✅ /components/dashboards/FounderDashboard.tsx
- ✅ /components/SeamlessLandingPage.tsx
- ✅ /components/dashboards/TalentDashboard.tsx

## Pattern to Replace:

```
} from 'lucide-react';
```

Replace with:

```
} from 'lucide-react';
```

## Remaining Files to Update (100+ files):

All tsx files in the project that import from 'lucide-react' without a version.

The stable version 0.400.0 fixes the memory/worker error from version 0.546.0.
