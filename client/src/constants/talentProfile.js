/** Minimum profile depth (0–100) before primary talent actions (e.g. browse / apply). */
export const TALENT_ACTIONS_MIN_COMPLETION = 80;

/** Profiles below this completion % are omitted from founder-facing browse lists. */
export const TALENT_BROWSE_MIN_COMPLETION = 70;
