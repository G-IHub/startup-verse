Open `client/src/check-backend-status.html` in a browser (file:// or static host).

Pass the API base via the `?api=` query string, e.g. `?api=https://api.example.com/api/v1`.

You should see a successful `/health` response when the server is running.
