import React from "react";

/**
 * Tutorials and tour progress are intentionally disabled project-wide.
 * Keep component API for compatibility with existing imports.
 */
export default function InteractiveTour() {
  return null;
}
